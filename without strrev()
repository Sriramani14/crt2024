without strrev()
#include <stdio.h>
#include<string.h>
int main() {
    char s[10],r[10];
    int n,i,j=0;
    scanf("%s",s);
    for(n=0;s[n]!='\0';n++);
    for(i=n-1;i>=0;i--){
        r[j]=s[i];
        j++;
    }
    r[j]='\0';
   printf("%s",r);
    return 0;
}
