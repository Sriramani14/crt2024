without strcpy()

#include <stdio.h>
#include<string.h>
int main() {
    char s[10],r[70];
    int i;
    scanf("%s",s);
   for(i=0;s[i]!='\0';i++)
 {
     r[i]=s[i];
 }
    printf("%s",r);
 
    return 0;
}
